## 这节课你要拿到什么

- 一套 Key 分离和命名规则
- 一张调用记录表
- 一组预算分级和降档规则
- 一个把真实调用反哺模型地图的回看节奏

> 上节课你画好了模型地图，这节课解决接入后的管理问题：Key 怎么管、成本怎么控、记录怎么留，以及这些记录怎样帮助你下次选模型。

## 第一步：把 Key 分开（⏱ 10 分钟）

只要开始接 API，就要先把测试、日常和自动化调用隔离开。优先使用供应商提供的多个 Key、项目或服务账号；如果平台不支持细粒度额度，再用自建网关的虚拟 Key、预算告警或独立项目补上控制。

### 按 3 类用途做隔离

| 凭据角色 | 用途 | 预算控制 | 禁止事项 |
|----------|------|----------|----------|
| test-[你的名字] | 开发测试、试新模型、调提示词 | 单独项目、额度或告警 | 不接正式工作流 |
| daily-[你的名字] | 日常工作流、正式任务 | 按月预算与告警 | 不跑批量实验 |
| auto-[你的名字] | 自动化流程、批量任务 | 按任务量设上限并限速 | 不直接执行高风险动作 |

如果供应商只提供一个账号级 Key，不要假装已经隔离：至少在调用端分环境、分日志，并明确哪一层真正执行预算和权限控制。

### Key 存放规则

- 存在密码管理器里，不要存在聊天记录或截图里
- 不要写进公开代码仓库、教程素材或共享文档
- 如果用 `.env` 文件，确保 `.gitignore` 里有 `.env`
- 新工具先接 `test` Key，连续稳定后再接 `daily` 或 `auto`

✅ 完成标准：你能指出测试与正式调用在哪里被隔离；如果供应商支持多个 Key、项目或服务账号，至少拆出 test 和 daily 两套凭据。

## 第二步：建一张调用记录表（⏱ 10 分钟）

不需要一开始上复杂平台，用飞书多维表格就够。

### 在飞书多维表格里建表

新建一个多维表格，命名为“API 调用记录”，加这些字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| 日期 | 日期 | 调用日期 |
| 任务 | 单选 | 摘要 / 写作 / 提取 / 分析 / 批量 / 其他 |
| 模型 | 单选 | 真实调用的模型名或网关别名 |
| 模型角色 | 单选 | 默认 / 高质量 / 低成本 / 回退 |
| Key 类型 | 单选 | test / daily / auto |
| 输入量 | 数字 | 大约多少 token，粗估即可 |
| 输出量 | 数字 | 大约多少 token |
| 耗时 | 数字 | 秒 |
| 预算档 | 单选 | 低 / 中 / 高 |
| 成本 | 数字 | 元或美元，币种保持一致 |
| 结果可用 | 单选 | 是 / 否 / 需返工 |
| 返工量 | 数字 | 需要改多少分钟 |
| 异常原因 | 文本 | 失败、限速、超时、格式错等 |

### 示例数据

| 日期 | 任务 | 模型角色 | Key | 输入量 | 输出量 | 耗时 | 预算档 | 成本 | 结果可用 | 返工量 |
|------|------|----------|-----|--------|--------|------|--------|------|----------|--------|
| 3/25 | 周报摘要 | 默认 | daily | 2000 | 800 | 15s | 中 | 以账单为准 | 是 | 5 分钟 |
| 3/25 | 竞品分析 | 高质量 | daily | 8000 | 3000 | 45s | 高 | 以账单为准 | 需返工 | 25 分钟 |
| 3/26 | 批量分类 | 低成本 | auto | 50000 | 5000 | 30s | 低 | 以账单为准 | 是 | 10 分钟抽查 |

先积累一批可复查记录，再按任务类型汇总。样本还少时只描述“这几次发生了什么”，不要直接下“某模型一定更省”或“某任务一定该降档”的结论。

✅ 完成标准：你有一张调用记录表，至少记了 3 条真实数据。

## 第三步：给不同任务设预算档（⏱ 10 分钟）

按任务设预算，这样选模型就有边界。

| 预算档 | 任务类型 | 默认模型角色 | 单次上限怎么定 | 超过后怎么办 |
|--------|----------|--------------|----------------|--------------|
| 低预算 | 批量分类、预处理、简单摘要 | 低成本模型 | 以你的月预算倒推 | 降采样、分批、抽查 |
| 中预算 | 内容提纲、方案框架、信息整理 | 默认模型 | 看返工时间是否可接受 | 优化提示词或换默认模型 |
| 高预算 | 复杂分析、对外高质量内容、关键文档 | 高质量模型 | 看交付价值和风险 | 需要人工确认和复盘 |

预算档不是固定金额，而是决策边界。不同平台、模型、地区和缓存规则都会变化，具体价格只以官方当前页面和你的账单为准。

在调用记录表里加一个“预算档”字段，每周汇总看看各档花了多少。

✅ 完成标准：你的调用记录表里有预算档字段，知道每类任务的成本上限和超限动作。

## 第四步：写降档和升档规则（⏱ 10 分钟）

预算治理不是一味省钱，而是把钱花在值得的地方。

### 降档规则

出现这些情况，考虑从默认模型降到低成本模型：

- 任务是批量提取、分类、清洗，人工抽查即可
- 输出只作为中间材料，不直接交付
- 一批连续记录显示默认模型质量明显过剩，且低成本候选通过了抽查
- 该任务成本超过预算，但返工率没有明显改善

### 升档规则

出现这些情况，考虑从默认模型升到高质量模型：

- 输出要对外发布、给客户或进入关键决策
- 默认模型连续多次遗漏事实或格式不稳定
- 人工返工时间明显高于模型成本差异
- 任务包含复杂推理、多约束方案或严肃代码审查

把这些规则写进模型地图，不要只写在脑子里。

✅ 完成标准：每个常用任务都有“什么时候降档”和“什么时候升档”的说明。

## 第五步：给高风险动作做日志（⏱ 5 分钟）

只要流程里涉及这些动作，就一定要留日志并人工确认：

- 写文件，尤其是覆盖已有内容
- 发消息，如邮件、群通知、客户回复
- 调外部接口，如支付、数据同步、生产系统
- 批量更新数据
- 读取或处理包含 secrets、客户资料、合同、私有仓库的上下文

日志至少记录：

```text
触发人：[你的名字]
执行时间：[日期时间]
模型角色：[默认/高质量/低成本/回退]
模型名或网关别名：[实际调用项]
动作：[做了什么]
输入摘要：[大概内容，不贴敏感原文]
输出摘要：[大概结果]
是否人工确认：[是/否]
是否需要复盘：[是/否]
```

个人用户可以直接在飞书文档里建一个“高风险操作日志”页面。等以后做自动化时，再把日志接进工具或网关。

✅ 完成标准：你知道哪些动作需要留日志，有一个记录的地方。

## 固定回看节奏

回看频率按调用量和风险定：高频自动化可每周看，低频个人使用可按月看；发生异常账单、模型切换或接口变更时立即看。按这个顺序检查：

1. 哪类任务花费最高
2. 哪个任务返工量最高
3. 哪些失败来自模型、哪些来自提示词或输入质量
4. 有没有低预算任务误用了高质量模型
5. 有没有高价值任务为了省钱导致返工更多
6. 模型地图是否需要调整

回看不是为了追责，而是为了让选型表越来越接近真实工作。

## 个人用户的 5 条最小治理规则

直接照抄：

1. 所有实验调用都先走测试环境或 test 凭据，不直接碰正式流程
2. 新流程先通过代表性样本和失败场景验证，再接入 daily 凭据；不要把固定次数当作安全保证
3. 批量和自动化流程单独走 auto 凭据、独立项目或网关虚拟 Key，并设置预算与限速
4. 任何涉及外发、写入、支付、批量修改的动作都必须人工确认
5. 按调用量和风险设固定回看节奏，把成本、返工和异常写回模型地图

## 演练示例：小张第一次接 API

> 以下人物和数据为教学示例，用于展示记录方法，不代表真实用户效果。

小张是产品经理，想用 API 自动生成竞品周报。他一开始直接把同一个 Key 接进所有工具。

### 第 1 周：没做治理

| 问题 | 发生了什么 |
|------|------------|
| Key 混用 | 测试和正式用同一个 Key，测试时把额度烧完了，正式流程断了 |
| 没有记录 | 不知道哪次调用花了多少钱，月底账单吓一跳 |
| 没有预算 | 简单摘要用了高质量模型，批量任务没有成本边界 |
| 没有返工记录 | 只知道“贵”，不知道贵是否减少了人工修改 |

### 第 2 周：按这节课的方法补治理

| 改进 | 做了什么 |
|------|----------|
| 凭据隔离 | 按平台能力拆了 test / daily / auto 凭据或项目 |
| 调用记录 | 记录任务、模型角色、成本、返工量和异常 |
| 预算分级 | 批量摘要先走低成本，深度分析才升档 |
| 固定回顾 | 按调用量检查记录，找到值得进一步验证的降档候选 |
| 回填模型地图 | 把“竞品摘要可降档、客户方案需升档”写成规则 |

示例结论：治理不是只看省钱，而是让账单、返工和模型切换都有记录可查。

## 常见翻车

### 翻车 1：Key 写进公开仓库

❌ 错误做法：把 API Key 直接写在代码里，推到公开仓库。

为什么错：公开仓库很容易被扫描，Key 泄露后会带来账单和数据风险。

✅ 正确做法：Key 存在 `.env` 或密钥管理器里，公开仓库不要包含密钥和真实配置。

### 翻车 2：所有任务都用高质量模型

❌ 错误做法：觉得“贵的肯定好”，所有任务都用高质量模型。

为什么错：简单摘要、分类和清洗不需要同一档能力。你省下的预算应该留给关键任务。

✅ 正确做法：按预算档和任务价值分配模型。低预算任务先用低成本模型，关键交付才升档。

### 翻车 3：从不回看调用记录

❌ 错误做法：接了 API 之后就不管了，月底看账单才发现超支。

为什么错：很多无效调用、重复调用、参数写错和模型错配会悄悄烧钱。

✅ 正确做法：按调用量和风险设回看频率；异常账单、接口变更或模型切换时立即检查。

## 自检清单

- [ ] 测试与正式调用已隔离；平台支持时至少有 test 和 daily 两套凭据或项目
- [ ] 建了一张调用记录表，至少记了 3 条真实数据
- [ ] 给不同任务设了预算档（低 / 中 / 高）
- [ ] 写清了升档、降档和回退规则
- [ ] 知道哪些高风险动作需要留日志
- [ ] 设了适合调用量和风险的回看提醒，并把结论写回模型地图

## 本课落地动作

1. 去你常用的模型平台，优先创建 test 和 daily 两套独立凭据或项目；若平台不支持，就在网关或调用端补隔离、预算和告警
2. 在飞书多维表格里建一张调用记录表，记录今天的 3 次调用
3. 给你的 3 类高频任务写预算档、升档条件和降档条件

> API 接好了，最后一课用系统评测把选型彻底落地：从“感觉还行”变成“数据说话”。
