## 1. 一个文档就够了

新手不需要先搭四层知识库。在飞书文档、Notion 或本地文档中新建一页“我的 AI 模板”，把本次有效指令保存进去即可。

先有一个真正常用的模板，再考虑分类。

## 2. 模板至少保留四项

记录模板名称、适用场景、完整指令和检查清单。

把每次会变化的内容写成 `{{材料}}`、`{{语气}}`、`{{字数}}` 等变量，这样下次只替换材料，不需要重写整段。

## 3. 本课练习

保存模板 v1，并补一句“这个模板最容易错在哪里”。

这句提醒通常比增加更多提示词更有价值，因为它会直接指导下一次人工检查。
